<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.carlos.proyecto2ab">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu" />
        <activity android:name=".presupuesto" />
        <activity android:name=".gastos" />
        <activity android:name=".iniciar" />
        <activity android:name=".registrar" />
        <activity android:name=".consejos" />
        <activity android:name=".perfil" />
        <activity android:name=".ahorro" />
        <activity android:name=".balance"></activity>
    </application>

</manifest>
